- config:
    name: json请求头
    request:
      base_url: http://172.16.1.20:8000
      headers:
        Content-Type: application/json;charset=UTF-8
        authorization: Basic ZWxhc3RpYzoxMjM0NTY=
- test:
    extract:
    - token: content.token
    name: 登录正常用例
    parameters:
    - username-password:
      - - admin
        - 123456
      - - admin
        - 1234567
    request:
      json:
        password: $password
        username: $username
      method: POST
      url: u2s2/u2s/user/login
    validate:
    - check: status_code
      comparator: equals
      expected: 200
    variables:
    - token: content.token
- test:
    name: 获取目标检索展示列表
    request:
      data:
        name: ''''''
      headers:
        Content-Type: application/x-www-form-urlencoded
        token: $token
      method: POST
      url: /u2s2/u2s/camera/getCameraListTree
    validate:
    - check: content.msg
      comparator: equals
      expected: success
